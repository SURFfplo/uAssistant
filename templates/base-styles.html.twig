<style>
    /* Div */
    .top-buffer {
        margin-top:20px;
    }
    .right-buffer {
        margin-right:10px;
    }

    .border-card {
        border: 0px;
    }

    /* BG Color */
    .bg-primary {
        background-color: #{{ my_function('PRIMARY_COLOR') }} !important;
    }
    .bg-secondary {
        background-color: #c3c3c3 !important;
    }

    /* Button */
    .button {
        display:block;
        width:50px;
        height:50px;
        border-radius:50px;
        font-size:30px;
        color:#{{ my_function('PRIMARY_TEXT_COLOR') }};
        padding-top: 2px;
        text-align:center;
        background:#{{ my_function('PRIMARY_COLOR') }};
        -webkit-box-shadow: 0px 1px 3px 0 rgba(0,0,0,0.2);
        box-shadow: 0px 1px 3px 0 rgba(0,0,0,0.2);
    }

    .button:hover {
        color:#{{ my_function('SECONDARY_TEXT_COLOR') }};
        text-decoration:none;
        background:#{{ my_function('SECONDARY_COLOR') }};
    }

    .notification {
        display:block;
        width:50px;
        height:50px;
        border-radius:50px;
        font-size:30px;
        color:#{{ my_function('PRIMARY_TEXT_COLOR') }};
        padding-top: 2px;
        text-align:center;
    }

    .notification:hover {
        color:#{{ my_function('SECONDARY_TEXT_COLOR') }};
        text-decoration:none;
    }

    label.required:after {
        content: "*";
        color: red;
    }
</style>